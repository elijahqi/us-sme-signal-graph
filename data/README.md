# Local data boundary

This directory is excluded from version control except for this notice.

The raw folder may contain third-party pages and the processed folder may contain row-level derived records used locally. Neither is covered by the repository Apache-2.0 license.

Run make baseline to reconstruct the current baseline locally. Public releases contain code, schemas, methods, aggregate statistics, and hashes rather than third-party row-level data.
