# Continuation after a diagnosed max-pass identity failure

This execution amendment was recorded on 2026-09-20 UTC after inspecting max-pass batch 27 and before submitting batch 28. It supplements the [max protocol](glm_max_cross_review_protocol_v0_1.md) and is not a prespecified rule. The frozen prompt, model, max effort, schema, evidence, reference labels, permutation, batches, and original protocol fingerprint remain unchanged.

Batch 27 completed with HTTP 200 and the requested GLM-5.3/max settings. Its complete JSON contained 20 unique result IDs: 19 requested IDs and one unexpected ID, leaving one requested ID absent. The original validator correctly stopped the worker. This is a result-identity failure, not an eligible transport retry. No interpretation of evidence, labels, or GPT agreement was needed to diagnose it.

After diagnosis, the entire 20-record batch is closed as `quarantined_identity_failure`. None of its judgments enters concordance analysis, including the 19 records with matching IDs. The unexpected ID is not corrected or mapped to the missing one. No new model call is made for that batch. Raw response and stream bytes, transport trace, original failure journal, model usage, and any earlier attempt artifacts are retained with fingerprints. The closed failure records this amendment's fingerprint separately from the original frozen protocol.

This closure allows the already-authorized submission of subsequent unattempted batches. It does not turn failed records into valid, unclear, or omitted judgments. Complete submission and valid-judgment coverage remain distinct. Automatic retries still apply only to the existing bounded transport policy. Future malformed or identity-ambiguous results stop for diagnosis; the supervisor does not automatically quarantine them. Only an explicitly diagnosed complete response with duplicate or unexpected IDs is eligible for this offline closure. Incomplete responses, API errors, wrong models, changed inputs, and fingerprint mismatches are not eligible.

Before resuming and during analysis, verify the original failed journal, unchanged raw artifacts, max transport settings, original input/reference/protocol fingerprints, and this amendment. Refuse a quarantined batch if an accepted result file exists. Preserve its full planned-row denominator and coverage by original GPT class. Report the number of raw output objects in failed batches separately from returned schema-valid/invalid records with usable identity mapping; do not add an unexpected ID to the study population.

If every planned batch is eventually either resolved or closed as such a failure, the analysis may report `complete_with_failed_batches`, meaning submission finished with excluded failed batches. Report planned, valid, schema-invalid, omitted, quarantined-failed, other failed/uncertain, and unattempted counts separately. Concordance uses only schema-valid paired judgments. It is neither accuracy nor human validation, and whole-batch exclusion may affect representativeness. This amendment does not change any human or entity-resolution validation gate.
