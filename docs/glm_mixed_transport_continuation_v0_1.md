# Continuation after a rate limit followed by exhausted connection retries

This retrospective execution amendment was recorded on 2026-09-22 UTC after max-pass batch 282 exhausted its original request and two authorized retries, before submitting batch 283. The existing connection-only amendment requires three connection resets and does not cover this sequence. Its text, fingerprints and earlier closures remain unchanged.

The first attempt returned HTTP 429 with provider code 1302 and the message `Rate limit reached for requests`. Each of the two retries ended in a captured `ConnectionResetError` and an API-error termination reporting a stream idle timeout. All three outgoing requests used GLM-5.3 with max effort. The preserved journals show at least the required 30- and 60-second backoffs. Local power records show sleep and dark-wake cycles overlapping the retries; this supports suspension as a contributor without establishing an exclusive cause.

After explicit offline diagnosis, close the entire planned batch as `quarantined_exhausted_mixed_transport_failure`. Accept no judgments, make no fourth request, and preserve every original response, stream, transport trace, failure journal and prior-attempt fingerprint. The closure requires exactly this sequence: a first HTTP 429 response explicitly identifying request-rate limit code 1302, followed by two diagnosed connection-reset failures. Authentication, exhausted quota, successful malformed output, other HTTP errors, changed provenance, missing attempts and accepted result files do not qualify. This is not automatic closure; future failures still stop for diagnosis.

Subsequent unattempted batches may proceed on an awake, connected host under the existing serial pacing and bounded retry policy. No frozen input, reference, prompt, schema, model, effort, batch ordering or original protocol fingerprint changes. The separate amendment fingerprint is verified during continuation and analysis. Report these failed planned rows separately from connection-only failures, identity failures, invalid rows and omissions. Interrupted output-object counts and server-side usage remain unknown, and the failed batch supplies no paired judgments. Complete submission remains distinct from complete valid coverage, accuracy and human validation.
