# Continuation after exhausted connection retries

This retrospective execution amendment was recorded on 2026-09-20 UTC after max-pass batch 49 exhausted its original request and two authorized automatic retries, and before submission of batch 50. It supplements the max protocol and does not change frozen evidence, reference labels, prompts, output schema, model, max effort, ordering, or the original protocol fingerprint.

All three attempts ended with a captured connection-reset failure and an interrupted client response. The last client error reported a stream idle timeout. Local power records show lid-close sleep during the first attempt and repeated sleep/wake cycles overlapping the retries. These records support local suspension as a contributor; they do not exclude other network or service causes. Interrupted output supplies no accepted research judgments, and server-side usage remains unknown.

After explicit diagnosis, retain the whole 20-record batch as `quarantined_exhausted_connection_failure`. Preserve each raw response, stream, transport trace, original failed journal, and retry history with hashes. Make no fourth request, repair no labels, and salvage no interrupted judgments. Later unattempted batches may proceed under the existing serial, max-effort and pacing rules. The automatic supervisor still stops on retry exhaustion and does not perform this closure automatically.

The offline closure requires exactly three preserved failed attempts, each with a recorded `ConnectionResetError`, an error termination, verified max-model transport and pacing, and matching frozen provenance. It refuses an accepted result file. Authentication failures, exhausted quota, malformed successful outputs, incomplete histories, or other undiagnosed failures do not qualify under this amendment. Analysis verifies all three attempts and the amendment fingerprint, retains all failed rows in the planned denominator and GPT-class coverage, and reports exhausted-connection failures separately from identity failures, schema-invalid records and omissions. Counts of interrupted output objects and server-side usage remain unknown rather than zero.

To support continued execution, a temporary idle-sleep assertion may be attached to the worker and expires when it exits. It does not change system sleep preferences or override closing the lid. Continued local execution requires an awake, network-connected host. This operational change provides no additional model or human evidence. A completed submission pass with failures is not a complete set of valid judgments or an accuracy study.
